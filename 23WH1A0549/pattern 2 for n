#include<stdio.h>
void pattern(int);

int main(){
    int n;
    printf("enter the number");
    scanf("%d",&n);
    pattern(n);
}
void pattern(int n){
    int i,j;
    for(i=1;i<=n;i++){
        for(j=1;j<=i;j++){
            printf("%d ",i);
        }
        printf("\n");
    }
}
