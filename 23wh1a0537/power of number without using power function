// to find power of a number without power function 
#include <stdio.h>

int main() {
    int base,exponent,i;
    printf("enter base and exponent \n");
    scanf("%d %d",&base,&exponent);
    
     int temp=base;
    for(i=2;i<=exponent;i++)
      base*=temp;
 printf("%d^%d=%d",temp,exponent,base);
    return 0;
}
