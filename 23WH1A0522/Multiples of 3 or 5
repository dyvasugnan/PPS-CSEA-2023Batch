#include <stdio.h>
void main(){
    int num,sum=0;
    printf("enter an number: ");
    scanf("%d",&num);
    for(int i=1;i<num;i++){
        if(i%3==0 || i%5==0){
            sum=sum+i;
        }
    }
    printf("sum of multiples of 3 or 5: %d",sum);
}
