// program on leapyear
#include<stdio.h>
int main(){
int num;
printf("enter a year");
scanf("%d",&num);
if (num%4==0 && num%400==0 && num%100!=0)
printf("it is a leapyear");
else
printf(" it is not a leapyear");
return 0;
}
