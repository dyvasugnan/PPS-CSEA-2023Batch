#include<stdio.h>
int swap(int*,int*);
int main(){
    int a=10,b=12;
    printf("a=%d and b=%d",a,b);
    swap(&a,&b);
    printf("\na=%d and b=%d",a,b);
}
int swap(int *x,int *y){
    int temp;
    temp=*x;
    *x=*y;
    *y=temp;
return *x,*y;
}
