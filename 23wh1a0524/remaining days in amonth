#include<stdio.h>
int main(){
   int days,months,remaining_days;
   printf("enter no of days");
scanf("%d",&days);
   months=days/30;
   remaining_days=days%30;
   printf("months=%d,remaining_days=%d",months,remaining_days);
      return 0;
         }
