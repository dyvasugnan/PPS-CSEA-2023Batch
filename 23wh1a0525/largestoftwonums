/*this programme is to find largest of two numbers*/
#include<stdio.h>
int main() {
             int num1,num2,result;
            printf("give two numbers");
            scanf("%d%d",&num1,&num2);
           result = (num1>num2)? num1:num2
         printf("%d",result);
        return 0;
}
            
