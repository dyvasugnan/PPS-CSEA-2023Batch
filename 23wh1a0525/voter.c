/*this programme is to find whether a person is eligible for voting or not*/
#include<stdio.h>
int main() {
            int age;
           printf("give age");
           scanf("%d",&age);
          if (age>=18)
               printf("eligible for voting");
          else
               printf("not eligible for voting");
return 0;
}
