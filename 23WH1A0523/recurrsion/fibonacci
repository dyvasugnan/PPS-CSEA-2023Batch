//nth term in the fibonacci sequence
#include<stdio.h>
int fibonacci(int);
int main(){
	int num;
	scanf("%d",&num);
	int res=fibonacci(num);
	printf("%d",res);
}
int fibonacci(int n){
	if(n==0 || n==1)
		return n;
	else 
		return fibonacci(n-1) + fibonacci (n-2);
}
