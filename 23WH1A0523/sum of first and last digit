#include<stdio.h>
int main(){
int num;
int sum=0;
printf("enter any number");
scanf("%d",&num);
while(num>0){
	int first_digit=(num%1000)/100;
	int last_digit=num%10;
	sum=first_digit+last_digit;
	printf("%d",sum);
	}
return 0;
}
